OPTIONS (SKIP = 1)
LOAD DATA
-- CHARACTERSET UTF8
TRUNCATE
INTO TABLE SAP_TR_AR_GRP_CLIENT
FIELDS TERMINATED BY ';' OPTIONALLY ENCLOSED BY '"'
TRAILING NULLCOLS
(
  GRP_SOURCE, 
  GRP_SOURCE_DESC,
  GRP_CIBLE,
  GRP_CIBLE_DESC 
)

