OPTIONS (SKIP = 1)
LOAD DATA
-- CHARACTERSET UTF8
TRUNCATE
INTO TABLE SAP_TR_COMPTES_GENE
FIELDS TERMINATED BY ';' OPTIONALLY ENCLOSED BY '"'
TRAILING NULLCOLS
(
  CPTE_SOURCE, 
  CPTE_SOURCE_DESC,
  CPTE_CIBLE,
  CPTE_CIBLE_DESC 
)
