OPTIONS (SKIP = 1)
LOAD DATA
-- CHARACTERSET UTF8
TRUNCATE
INTO TABLE SAP_TR_AGENT
FIELDS TERMINATED BY ';' OPTIONALLY ENCLOSED BY '"'
TRAILING NULLCOLS
(
  AGENT_SOURCE, 
  AGENT_SOURCE_DESC,
  AGENT_CIBLE,
  AGENT_CIBLE_DESC 
)
